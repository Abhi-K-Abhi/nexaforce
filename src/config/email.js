/* ═══════════════════════════════════════════════════
   config/email.js

   SETUP INSTRUCTIONS (5 minutes):
   1. Go to https://emailjs.com → create free account
   2. Add Email Service (connect Gmail) → copy Service ID
   3. Create Email Template → copy Template ID
   4. Go to Account tab → copy Public Key
   5. Replace the three placeholder strings below
═══════════════════════════════════════════════════ */

export const EMAIL_CONFIG = {
  SERVICE_ID:  'YOUR_SERVICE_ID',   // e.g. "service_abc123"
  TEMPLATE_ID: 'YOUR_TEMPLATE_ID',  // e.g. "template_xyz789"
  PUBLIC_KEY:  'YOUR_PUBLIC_KEY',   // e.g. "abcDEFghiJKL12345"
}
